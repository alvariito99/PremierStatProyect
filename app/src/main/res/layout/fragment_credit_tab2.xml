<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CreditTab2Fragment">

    <!-- Logo de la Premier League -->
    <ImageView
        android:id="@+id/logoPremier"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/premierleague"
        android:contentDescription="@string/logo_premier_description"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/tvTab2"
        app:layout_constraintVertical_bias="0.3"/>

    <!-- Título o mensaje -->

    <!-- Botón de Inicio -->
    <TextView
        android:id="@+id/tvTab2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:padding="16dp"
        android:text="Bienvenido a la App de la Premier League"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/logoPremier" />

    <Button
        android:id="@+id/btnIniciar"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:text="Iniciar"
        android:textSize="18sp"
        android:backgroundTint="@color/Gris"
        android:textColor="@android:color/white"
        android:paddingVertical="12dp"
        android:drawableStart="@drawable/ic_play"
        android:drawablePadding="10dp"
        app:layout_constraintTop_toBottomOf="@id/tvTab2"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.2"/>

</androidx.constraintlayout.widget.ConstraintLayout>
